import axios from 'axios'

const defaultTime = {
  possibleTimes: [],
  selectedTime: {}
}

export default function(state = defaultTime, action) {
  switch (action.type) {
    default:
      return state
  }
}
